name: Deploy Flutter Web → GitHub Pages (dual)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      # ── Versión 1: clientes/invitados ──────────────────────────────
      - name: Build web — clientes (GuestHome)
        run: flutter build web --release --base-href "/serviexpressweb/"

      - name: Guardar build clientes
        run: |
          mkdir -p deploy/serviexpressweb
          cp -r build/web/. deploy/serviexpressweb/

      # ── Versión 2: operadores (login completo) ─────────────────────
      - name: Build web — operadores (Login)
        run: flutter build web --release --base-href "/serviexpressweb-login/" --dart-define=LOGIN_MODE=true

      - name: Guardar build operadores
        run: |
          mkdir -p deploy/serviexpressweb-login
          cp -r build/web/. deploy/serviexpressweb-login/

      # ── Deploy ambas a la rama gh-pages ───────────────────────────
      - name: Crear .nojekyll
        run: touch deploy/.nojekyll

      - name: Deploy a GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./deploy
          force_orphan: true
