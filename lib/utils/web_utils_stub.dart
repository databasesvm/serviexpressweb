// Stub para plataformas no-web (Android, iOS, desktop).
// No hace nada — el código que llama a openWhatsAppTab ya está
// protegido por kIsWeb, así que este método nunca se ejecuta.
void openWhatsAppTab(String url) {}
