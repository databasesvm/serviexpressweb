<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Opción B2: requerido en Android 14+ para foreground service tipo dataSync -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- USE_EXACT_ALARM eliminado: solo permitido para apps de alarma/calendario.
         SCHEDULE_EXACT_ALARM (que sí permite pedir permiso al usuario) es suficiente. -->

    <!-- NUEVO: necesario para poder PEDIR la exención de optimización de
         batería. Sin este permiso, la app no puede ni mostrar el diálogo
         del sistema que excluye a ServiExpress de Doze/ahorro de batería.
         Esto es la causa más común de que las notificaciones no lleguen
         con la app minimizada, sobre todo en Xiaomi/Huawei/Oppo. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- SYSTEM_ALERT_WINDOW eliminado: Google Play lo restringe a apps de
         burbujas de chat/accesibilidad. Las notificaciones heads-up con
         canal de importancia HIGH cubren el mismo caso de uso. -->

    <!-- USE_FULL_SCREEN_INTENT eliminado: restringido en Android 14+ para
         apps que no son alarmas o llamadas. Notificaciones HIGH channel
         son suficientes para las alertas de ServiExpress. -->

    <!-- REQUEST_INSTALL_PACKAGES eliminado: política de Google Play prohíbe
         que apps distribuidas en la tienda instalen sus propios APKs. -->
    <!-- Las actualizaciones se manejan vía Google Play in-app updates. -->

    <application
        android:label="ServiExpress"
        android:name=".ServiMotoApp"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep link: serviexpress://pedido?local=42 -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="serviexpress" android:host="pedido"/>
            </intent-filter>

            <!-- Share intent: texto compartido desde WhatsApp u otras apps -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: permitir canLaunchUrl / launchUrl para http y https -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <!-- Google Maps navigation -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>
</manifest>